#include <stdio.h>
#include <stdlib.h>

int main()
{/*(1)
   /*int i,n,som;
   som = 0 ;
   for (i=0 ; i<4 ; i++)
   {
       printf (" donnez un eniter");
       scanf("%d", &n) ;
       som += n;
   }
   printf ("Somme : %d\n", som);*/

   /*int i,n,som;
   som = 0 ;
    i=0;
   while (i<4)
   {
       printf (" donnez un eniter");
       scanf("%d", &n) ;
       som += n;
       i++;
   }
   printf ("Somme : %d\n", som);*/

   /*int i,n,som;

   som = 0;
   do {
       printf (" donnez un eniter");
       scanf("%d", &n) ;
       som += n;
       i++;
   } while (i < 4);
   printf ("Somme : %d\n", som);*/


/*(2)*/

/*
    int i, n, som;
    float moy;

    som = 0;
    for (i = 0; i >= 0; i++) {//tant que I>0 pour boucle infis
        printf ("Donnez un eniter ");
        scanf("%d", &n);
        if (n < 0)
            break;//si chiffre negatif arreter la boucle
        som += n;
    }
    moy = som / i;
    printf ("Moyenne : %f\n", moy);*/

/*(3)*/

}

